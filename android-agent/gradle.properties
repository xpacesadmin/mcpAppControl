org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# The project intentionally does not hardcode a workstation-specific JDK path.
# Gradle uses JAVA_HOME supplied by the task-scoped build environment, so the
# same source builds on the admin PC and the isolated Lab VM.
